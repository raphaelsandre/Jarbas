[project]
name = "jarbas"
version = "0.1.0"
description = "Jarbas Automation Core"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "pydantic-settings",
    "httpx>=0.28.1",
    "python-multipart",
    "faster-whisper>=1.2.1",
    "starlette>=1.6.0",
]
